# 00 旧包废止与使用方法

## 从本版本开始

- V2 `designs/`：LEGACY_REFERENCE
- 之前错误的 V3 `designs/`：LEGACY_REFERENCE
- V4 `designs/pages/`：唯一页面视觉 Source of Truth
- V4 `designs/motion/`：唯一宠物动效视觉规范
- V4 `designs/overview/`：仅索引，不可逐像素实现

## 文件名编码保险

如果 Codex/Windows 解压后中文文件名出现 mojibake：

直接使用：

`designs/pages_ascii/`

里面是完全相同的页面 PNG，但路径全为 ASCII。

## 禁止

- 从旧包截图继续开发
- 从 overview 裁图实现页面
- 把 PNG 当背景图贴进 Flutter
- 硬编码设计图里的时长、奖励、报告数字
- 为 Records / Growth 每个标签单独增加底部导航入口
