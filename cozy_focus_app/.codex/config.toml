# Cozy Focus V4.1 Visual Polish — Subagent-safe mode
# Primary/root model: Claude Sonnet 4.6 (select in Codex UI)
# Writer child: gemini_implementer
#
# One child at a time is deliberate: no parallel writers, no duplicated audits.

[agents]
enabled = true
max_concurrent_threads_per_session = 1
interrupt_message = true
